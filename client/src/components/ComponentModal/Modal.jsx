import React from 'react';

const Modal = ({ children }) => {
    return (
        <div className='modal__container'>
            Modal
            {children}
        </div>
    );
};

export default Modal;
